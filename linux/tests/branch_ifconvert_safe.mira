fn checksum(n) {
    mut sum = 0;
    mut i = 0;
    while (i <= n) {
        if ((i & 1) == 0) {
            sum = sum + i;
        } else {
            sum = sum - i;
        }
        i = i + 1;
    }
    sum
}

fn main() {
    mut result = checksum(1000);
    print(result);
}
