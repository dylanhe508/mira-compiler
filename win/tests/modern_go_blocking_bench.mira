fn main() {
    let start = clock();
    let first = go([] {
        sleep(120);
    });
    let second = go([] {
        sleep(120);
    });
    join-task(first);
    join-task(second);
    print(clock() - start);
}
